<% if(flatList) { %>
  <div style="display: grid; grid-template-columns: 400px 400px; column-gap: 120px">
    <% flatList.forEach((flat)=> { %>
      <div>
        <%- include ("./showHomeOneFlat.ejs",{misc, flat}) %>
      </div>
      <% }) %>
  </div>
  <% } %>
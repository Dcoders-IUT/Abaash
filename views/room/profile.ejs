<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%=room? room.name: 'FLAT NOT FOUND'%></title>
    <link rel="stylesheet" href="/css/single-room.css" type="text/css" />
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap");
      @import url("https://fonts.googleapis.com/css2?family=Montserrat:wght@500;600&display=swap");

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        /* font-family: Lato, Helvetica,sans-serif; */
        font-family: "Montserrat", sans-serif;
      }

      .container {
        width: 100vw;
        height: 95vh;
        background-image: linear-gradient(
            110deg,
            rgba(255, 255, 255, 0.7) 50%,
            transparent 50%
          ),
          url("/images/home5.jpg");
        background-size: 100% 100%;
        /* display: flex;
          align-items: center; */
      }

      .details {
        /* border:2px solid; */
        box-shadow: 0 5px 20px -6px #000;
        width: 25vw;
        height: fit-content;
        background-color: rgba(255, 255, 255, 0.7);
        color: #20247b;
        margin-left: 10vw;
        padding: 23px;
        position: fixed;
        top: 28vh;
        left: 3vw;
        border-radius: 8px;
      }

      .form-item {
        /* border-bottom: 0.1rem solid #f5f5f5; */
        margin: 7px;
      }

      .attribute {
        width: 88px;
        position: absolute;
      }

      .val {
        font-weight: 700;
        position: relative;
        left: 140px;
        color: #3f7fb7;
      }

      h1 {
        margin-bottom: 30px;
      }

      a {
        text-decoration: none;
      }

      .bb > a {
        color: whitesmoke;
        text-decoration: none;
      }

      .bb {
        appearance: none;
        background-color: #3f7fb7;
        border: 1px solid rgba(27, 31, 35, 0.15);
        border-radius: 6px;
        box-shadow: rgba(27, 31, 35, 0.1) 0 1px 0;
        box-sizing: border-box;
        color: #fff;
        cursor: pointer;
        display: inline-block;
        font-family: -apple-system, system-ui, "Segoe UI", Helvetica, Arial,
          sans-serif, "Apple Color Emoji", "Segoe UI Emoji";
        font-size: 14px;
        font-weight: 600;
        line-height: 20px;
        padding: 6px 16px;
        position: relative;
        text-align: center;
        text-decoration: none;
        user-select: none;
        -webkit-user-select: none;
        touch-action: manipulation;
        vertical-align: middle;
        white-space: nowrap;
        margin: 15px;
      }

      .bb:focus:not(:focus-visible):not(.focus-visible) {
        box-shadow: none;
        outline: none;
      }

      .bb:hover {
        background-color: #1e64a1;
      }

      .bb:focus {
        box-shadow: rgba(20, 35, 172, 0.4) 0 0 0 3px;
        outline: none;
      }

      .bb:disabled {
        background-color: #94d3a2;
        border-color: rgba(27, 31, 35, 0.1);
        color: rgba(255, 255, 255, 0.8);
        cursor: default;
      }

      .bb:active {
        background-color: #2c379d;
        box-shadow: rgba(20, 70, 32, 0.2) 0 1px 0 inset;
      }
    </style>
  </head>

  <body>
    <a href="../..">Home</a>
    <div class="container">
      <div class="details">
        <% if(room) { %>
        <h1><%=room.name%></h1>
        <div class="form-item">
          <label class="attribute">Type</label
          ><span class="val"><%=misc.roomText(room.type)%></span>
        </div>
        <div class="form-item">
          <label class="attribute">Area</label
          ><span class="val"><%=room.area%> square feet</sup></span>
        </div>
        <div class="form-item">
          <label class="attribute">Flooring</label
          ><span class="val"><%=misc.floorText(room.flooring) %></span>
        </div>
        <div class="form-item">
          <label class="attribute"> Flat</label>
          <span class="val"
            ><a href="../../flat/profile/<%=flat.flatID%>"
              ><%=flat.name%></a
            ></span
          >
        </div>
      </div>
      <% if(currentUser===flat.owner) { %>
      <button>
        <a href="../edit/<%=room.roomID%>">Edit Info</a>
      </button>
      <% } } else { %>
      <span>ROOM NOT FOUND!</span>
      <% } %>
    </div>
  </body>
</html>
